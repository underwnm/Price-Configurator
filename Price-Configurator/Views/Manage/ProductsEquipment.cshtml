@model Price_Configurator.ViewModels.ProductEquipmentViewModel
@{
    ViewBag.Title = "Manage Products Equipment";
}

<h2 class="text-center">Manage Product Equipment</h2>

<div class="span12">
    <table class="table table-responsive">
        <thead>
            <tr>
                <th>Add A New Equipment To Product</th>
                <th><button class="btn-primary btn">@Html.ActionLink("Add", "AddProductEquipment", "Manage")</button></th>
            </tr>
        </thead>
    </table>
    <br />
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Product</th>
                <th>Equipment</th>
                <th>Edit</th>
                <th>Remove</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var role in Model.CurrentProductEquipments)
            {
                <tr>
                    <td>@role.ProductId</td>
                    <td>@role.EquipmentId</td>
                    <td><button class="btn-warning btn">Edit</button></td>
                    <td><button class="btn-danger btn">Remove</button></td>
                </tr>
            }
        </tbody>
    </table>
</div>

<p>@Html.ActionLink("Back to manager.", "Index", "Manage")</p>